__pycache__/
*.py[cod]
.venv/

# Secrets - JANGAN pernah di-commit
.streamlit/secrets.toml

# Backup otomatis dari mode Seleksi Kolom (artefak lokal, tidak perlu di-commit)
*.xlsx.bak

# Dokumen Dataiku Flow Documentation lokal buat testing - tidak dipakai app
# (upload manual tiap sesi), dan isinya dokumentasi flow asli, jangan ke git.
*.docx

# Dump JSON hasil export_code.txt/dump_flow_via_notebook.py - isinya definisi
# recipe/dataset project Dataiku asli, sama kayak .docx di atas jangan ke git.
export_ecm.json
